diff --git a/priv/couchdb/schemas/queues.json b/priv/couchdb/schemas/queues.json
--- a/priv/couchdb/schemas/queues.json
+++ b/priv/couchdb/schemas/queues.json
@@ -29,6 +29,13 @@
                     "minimum": 15,
                     "type": "integer"
                 },
+                "language": {
+                    "description": "Optional lowercase BCP 47 prompt language override. When omitted, the caller/account prompt language is used.",
+                    "maxLength": 35,
+                    "minLength": 2,
+                    "pattern": "^[a-z]{2,3}(-[a-z0-9]{2,8})*$",
+                    "type": "string"
+                },
                 "media": {
                     "description": "Custom prompts to be played for the announcements",
                     "properties": {
