diff --git a/app.js b/app.js
--- a/app.js
+++ b/app.js
@@ -717,6 +717,73 @@
 				container.empty().append(template);
 
 				self.bindAccountSettingsEvents(template, accountSettingsData, widgetBlacklist);
+				self.bindForwardedCallConfirmation(template, accountSettingsData);
+			});
+		},
+
+
+		bindForwardedCallConfirmation: function(template, data) {
+			var self = this,
+				section = template.find('.forward-confirmation-settings'),
+				select = section.find('.forward-confirmation-language'),
+				save = section.find('.forward-confirmation-save'),
+				status = section.find('.forward-confirmation-status'),
+				accountSave = template.find('.account-settings-update'),
+				accountId = data.account.id,
+				current = _.get(data.account, 'call_forward_confirmation.language', ''),
+				strings = self.i18n.active().callflows.accountSettings.forwardConfirmation,
+				busy = false;
+
+			if (!monster.util.isAdmin() && !monster.util.isSuperDuper()) {
+				section.remove();
+				return;
+			}
+			select.val(current);
+			save.prop('disabled', true);
+			select.on('change', function() {
+				status.text('');
+				save.prop('disabled', busy || select.val() === current);
+			});
+			save.on('click', function() {
+				var selected = select.val(),
+				wasDisabled = accountSave.prop('disabled');
+				if (busy || self.accountId !== accountId || selected === current ||
+					!_.includes(['', 'en-us', 'he-il', 'ar-sa', 'es-es', 'fr-fr'], selected)) {
+					return;
+				}
+				busy = true;
+				select.prop('disabled', true);
+				save.prop('disabled', true);
+				accountSave.prop('disabled', true);
+				status.text(strings.saving);
+				function active() {
+					return self.accountId === accountId && template.closest('html').length > 0;
+				}
+				function finish(message) {
+					busy = false;
+					if (!active()) { return; }
+					select.prop('disabled', false);
+					save.prop('disabled', select.val() === current);
+					accountSave.prop('disabled', wasDisabled);
+					status.text(message);
+				}
+				self.callApi({
+					resource: 'account.patch',
+					data: { accountId: accountId, data: { call_forward_confirmation: { language: selected || null } } },
+					success: function(response) {
+						if (!active()) { busy = false; return; }
+						var returned = _.get(response, 'data.call_forward_confirmation.language', '');
+						if (returned !== selected) { finish(strings.failed); return; }
+						current = returned;
+						if (current) {
+							data.account.call_forward_confirmation = { language: current };
+						} else {
+							delete data.account.call_forward_confirmation;
+						}
+						finish(strings.saved);
+					},
+					error: function() { finish(strings.failed); }
+				});
 			});
 		},
 
@@ -942,6 +1009,9 @@
 
 				newData.blacklists = widgetBlacklist.getSelectedItems();
 
+				// Saved through its own PATCH; omission preserves the latest server value.
+				delete newData.call_forward_confirmation;
+
 				delete newData.extra;
 
 				self.callApi({
diff --git a/views/accountSettings.html b/views/accountSettings.html
--- a/views/accountSettings.html
+++ b/views/accountSettings.html
@@ -141,6 +141,23 @@
 				</div>
 
 				<div class="tab-pane" id="account_settings_misc">
+					<div class="settings-section forward-confirmation-settings">
+						<div class="section-title">{{ i18n.callflows.accountSettings.forwardConfirmation.title }}</div>
+						<div class="section-content">
+							<p>{{ i18n.callflows.accountSettings.forwardConfirmation.help }}</p>
+							<label for="forward_confirmation_language">{{ i18n.callflows.accountSettings.forwardConfirmation.language }}</label>
+							<select id="forward_confirmation_language" class="forward-confirmation-language">
+								<option value="">{{ i18n.callflows.accountSettings.forwardConfirmation.current }}</option>
+								<option value="en-us">English</option>
+								<option value="he-il" dir="rtl">עברית</option>
+								<option value="ar-sa" dir="rtl">العربية</option>
+								<option value="es-es">Español</option>
+								<option value="fr-fr">Français</option>
+							</select>
+							<button type="button" class="monster-button-primary forward-confirmation-save" disabled>{{ i18n.callflows.accountSettings.forwardConfirmation.save }}</button>
+							<p class="forward-confirmation-status" role="status" aria-live="polite"></p>
+						</div>
+					</div>
 					<div class="settings-section">
 						<div class="section-title">{{ i18n.callflows.accountSettings.musicOnHold.title }}</div>
 						<div class="section-content">
diff --git a/i18n/en-US.json b/i18n/en-US.json
--- a/i18n/en-US.json
+++ b/i18n/en-US.json
@@ -950,6 +950,16 @@
 		"__comment": "UI-1493: Adding accountSettings to advanced callflows app",
 		"__version": "3.22",
 		"accountSettings": {
+			"forwardConfirmation": {
+				"title": "Forwarded-call confirmation",
+				"help": "Choose the message language heard by a forwarded recipient before pressing 1 to accept. This choice applies to this account and keeps the existing confirmation requirement.",
+				"language": "Confirmation language",
+				"current": "Current behavior (restore existing prompt)",
+				"save": "Save language",
+				"saving": "Saving…",
+				"saved": "Saved. New forwarded calls will use this choice.",
+				"failed": "Could not confirm the saved language. Reload Account Settings before trying again."
+			},
 			"title": "Account Settings",
 			"callerId": {
 				"titleOutbound": "Account's External/Off-net Caller ID",
diff --git a/i18n/de-DE.json b/i18n/de-DE.json
--- a/i18n/de-DE.json
+++ b/i18n/de-DE.json
@@ -318,6 +318,16 @@
 			"closed_groups": "Geschlossene Gruppen"
 		},
 		"accountSettings": {
+			"forwardConfirmation": {
+				"title": "Bestätigung weitergeleiteter Anrufe",
+				"help": "Wählen Sie die Sprache der Ansage, die der Empfänger vor dem Drücken der 1 hört. Die Auswahl gilt für dieses Konto. Die bestehende Bestätigungspflicht bleibt erhalten.",
+				"language": "Sprache der Bestätigung",
+				"current": "Bisheriges Verhalten (bestehende Ansage wiederherstellen)",
+				"save": "Sprache speichern",
+				"saving": "Wird gespeichert…",
+				"saved": "Gespeichert. Neue weitergeleitete Anrufe verwenden diese Auswahl.",
+				"failed": "Das Speichern konnte nicht bestätigt werden. Laden Sie die Kontoeinstellungen neu, bevor Sie es erneut versuchen."
+			},
 			"callerId": {
 				"titleEmergency": "Anrufer-ID des Accounts für Notfälle",
 				"titleOutbound": "Ausgehender-Anrufer-ID des Accounts",
