diff --git a/kazoo_ei.h b/kazoo_ei.h
--- a/kazoo_ei.h
+++ b/kazoo_ei.h
@@ -91,6 +91,7 @@ struct ei_node_s {
 	int nodefd;
 	switch_atomic_t pending_bgapi;
 	switch_atomic_t receive_handlers;
+	switch_atomic_t event_stream_threads;
 	switch_memory_pool_t *pool;
 	ei_event_stream_t *event_streams;
 	switch_mutex_t *event_streams_mutex;
diff --git a/mod_kazoo.c b/mod_kazoo.c
--- a/mod_kazoo.c
+++ b/mod_kazoo.c
@@ -43,5 +43,6 @@ SWITCH_MODULE_LOAD_FUNCTION(mod_kazoo_load)
 	memset(&kazoo_globals, 0, sizeof(kazoo_globals));
 	kazoo_globals.pool = pool;
+	switch_thread_rwlock_create(&kazoo_globals.ei_nodes_lock, pool);
 	kz_set_hostname();
 	if(kazoo_load_config() != SWITCH_STATUS_SUCCESS) {
 		// TODO: what would we need to clean up here?
@@ -54,8 +55,6 @@ SWITCH_MODULE_LOAD_FUNCTION(mod_kazoo_load)
 	/* connect my internal structure to the blank pointer passed to me */
 	*module_interface = switch_loadable_module_create_module_interface(pool, modname);
 
-	switch_thread_rwlock_create(&kazoo_globals.ei_nodes_lock, pool);
-
 	switch_set_flag(&kazoo_globals, LFLAG_RUNNING);
 
 	/* create all XML fetch agents */
@@ -101,10 +100,12 @@ SWITCH_MODULE_SHUTDOWN_FUNCTION(mod_kazoo_shutdown) {
 	/* give everyone time to cleanly shutdown */
 	while (switch_atomic_read(&kazoo_globals.threads)) {
 		switch_yield(100000);
 		if (++sanity >= 200) {
-			switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_ERROR, "Unable to kill all threads, continuing. This probably wont end well.....good luck!\n");
-			break;
+			switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_CRIT,
+						  "Still waiting for %d mod_kazoo thread(s) before safe shutdown.\n",
+						  switch_atomic_read(&kazoo_globals.threads));
+			sanity = 0;
 		}
 	}
 
 	/* close the connection to epmd and the acceptor */
diff --git a/kazoo_event_stream.c b/kazoo_event_stream.c
--- a/kazoo_event_stream.c
+++ b/kazoo_event_stream.c
@@ -234,7 +234,8 @@ static void event_handler(switch_event_t *event) {
 }
 
 static void *SWITCH_THREAD_FUNC event_stream_loop(switch_thread_t *thread, void *obj) {
-    ei_event_stream_t *event_stream = (ei_event_stream_t *) obj;
+	ei_event_stream_t *event_stream = (ei_event_stream_t *) obj;
+	ei_node_t *ei_node;
 	ei_event_binding_t *event_binding;
 	switch_sockaddr_t *sa;
 	uint16_t port;
@@ -245,9 +246,8 @@ static void *SWITCH_THREAD_FUNC event_stream_loop(switch_thread_t *thread, void
 	short event_stream_keepalive;
 	short ok = 1;
 
-	switch_atomic_inc(&kazoo_globals.threads);
-
 	switch_assert(event_stream != NULL);
+	ei_node = event_stream->node;
 
 	event_stream_framing = event_stream->event_stream_framing;
 	event_stream_keepalive = event_stream->event_stream_keepalive;
@@ -352,6 +352,22 @@ static void *SWITCH_THREAD_FUNC event_stream_loop(switch_thread_t *thread, void
 
 	switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_DEBUG, "Shutting down erlang event stream %p\n", (void *)event_stream);
 
+	/* Serialize with request handlers, then remove this stream by identity.
+	 * Once it is unlinked, no new request can obtain the stream while its
+	 * bindings and pool are being destroyed. */
+	switch_mutex_lock(ei_node->event_streams_mutex);
+	{
+		ei_event_stream_t **link = &ei_node->event_streams;
+		while (*link && *link != event_stream) {
+			link = &(*link)->next;
+		}
+		if (*link == event_stream) {
+			*link = event_stream->next;
+		}
+	}
+	switch_clear_flag(event_stream, LFLAG_RUNNING);
+	switch_mutex_unlock(ei_node->event_streams_mutex);
+
 	/* unbind from the system events */
 	event_binding = event_stream->bindings;
 	while(event_binding != NULL) {
@@ -383,6 +399,7 @@ static void *SWITCH_THREAD_FUNC event_stream_loop(switch_thread_t *thread, void
 	/* clean up the memory */
 	switch_core_destroy_memory_pool(&event_stream->pool);
 
+	switch_atomic_dec(&ei_node->event_stream_threads);
 	switch_atomic_dec(&kazoo_globals.threads);
 
 	return NULL;
@@ -461,7 +478,18 @@ ei_event_stream_t *new_event_stream(ei_node_t *ei_node, const erlang_pid *from)
 	switch_threadattr_create(&thd_attr, event_stream->pool);
 	switch_threadattr_detach_set(thd_attr, 1);
 	switch_threadattr_stacksize_set(thd_attr, SWITCH_THREAD_STACKSIZE);
-	switch_thread_create(&thread, thd_attr, event_stream_loop, event_stream, event_stream->pool);
+
+	/* Account for the detached worker before it can run.  The node owner waits
+	 * for this per-node count before destroying event_streams_mutex and pool. */
+	switch_atomic_inc(&ei_node->event_stream_threads);
+	switch_atomic_inc(&kazoo_globals.threads);
+	if (switch_thread_create(&thread, thd_attr, event_stream_loop, event_stream, event_stream->pool) != SWITCH_STATUS_SUCCESS) {
+		*event_streams = event_stream->next;
+		switch_atomic_dec(&kazoo_globals.threads);
+		switch_atomic_dec(&ei_node->event_stream_threads);
+		switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_ERROR, "Unable to create erlang event stream thread\n");
+		goto cleanup;
+	}
 
 	return event_stream;
 
@@ -581,9 +609,15 @@ void bind_event_profile(ei_event_binding_t *event_binding, kazoo_event_ptr event
 
 void bind_event_profiles(kazoo_event_ptr event)
 {
-	ei_node_t *ei_node = kazoo_globals.ei_nodes;
+	ei_node_t *ei_node;
+
+	switch_thread_rwlock_rdlock(kazoo_globals.ei_nodes_lock);
+	ei_node = kazoo_globals.ei_nodes;
 	while(ei_node) {
-		ei_event_stream_t *event_streams = ei_node->event_streams;
+		ei_event_stream_t *event_streams;
+
+		switch_mutex_lock(ei_node->event_streams_mutex);
+		event_streams = ei_node->event_streams;
 		while(event_streams) {
 			ei_event_binding_t *bindings = event_streams->bindings;
 			while(bindings) {
@@ -592,8 +626,10 @@ void bind_event_profiles(kazoo_event_ptr event)
 			}
 			event_streams = event_streams->next;
 		}
+		switch_mutex_unlock(ei_node->event_streams_mutex);
 		ei_node = ei_node->next;
 	}
+	switch_thread_rwlock_unlock(kazoo_globals.ei_nodes_lock);
 }
 
 switch_status_t add_event_binding(ei_event_stream_t *event_stream, const char *event_name) {
diff --git a/kazoo_node.c b/kazoo_node.c
--- a/kazoo_node.c
+++ b/kazoo_node.c
@@ -126,6 +126,13 @@ static void destroy_node_handler(ei_node_t *ei_node) {
 	remove_event_streams(&ei_node->event_streams);
 	switch_mutex_unlock(ei_node->event_streams_mutex);
 
+	/* Event-stream workers use the node mutex while unlinking themselves.
+	 * Keep the node pool and mutex alive until every worker has completed. */
+	while ((pending = switch_atomic_read(&ei_node->event_stream_threads))) {
+		switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_DEBUG, "Waiting for %d event stream handlers to complete\n", pending);
+		switch_yield(100000);
+	}
+
 	remove_xml_clients(ei_node);
 
 	while (switch_queue_trypop(ei_node->received_msgs, &pop) == SWITCH_STATUS_SUCCESS) {
@@ -320,8 +327,7 @@ static void *SWITCH_THREAD_FUNC bgapi3_exec(switch_thread_t *thread, void *obj)
 
 	if(!switch_test_flag(ei_node, LFLAG_RUNNING) || !switch_test_flag(&kazoo_globals, LFLAG_RUNNING)) {
 		switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_ERROR, "Ignoring command while shuting down\n");
-		switch_atomic_dec(&ei_node->pending_bgapi);
-		return NULL;
+		goto done;
 	}
 
 	switch_malloc(send_msg, sizeof(*send_msg));
@@ -351,11 +357,12 @@ static void *SWITCH_THREAD_FUNC bgapi3_exec(switch_thread_t *thread, void *obj)
 		switch_safe_free(send_msg);
 	}
 
-	switch_atomic_dec(&ei_node->pending_bgapi);
-
+	done:
 	switch_safe_free(reply);
 	switch_safe_free(acs->arg);
+	switch_atomic_dec(&ei_node->pending_bgapi);
 	switch_core_destroy_memory_pool(&pool);
+	switch_atomic_dec(&kazoo_globals.threads);
 
 	return NULL;
 }
@@ -373,8 +380,7 @@ static void *SWITCH_THREAD_FUNC bgapi4_exec(switch_thread_t *thread, void *obj)
 
 	if(!switch_test_flag(ei_node, LFLAG_RUNNING) || !switch_test_flag(&kazoo_globals, LFLAG_RUNNING)) {
 		switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_ERROR, "Ignoring command while shuting down\n");
-		switch_atomic_dec(&ei_node->pending_bgapi);
-		return NULL;
+		goto done;
 	}
 
 	SWITCH_STANDARD_STREAM(stream);
@@ -410,15 +416,16 @@ static void *SWITCH_THREAD_FUNC bgapi4_exec(switch_thread_t *thread, void *obj)
 		switch_safe_free(send_msg);
 	}
 
-	switch_atomic_dec(&ei_node->pending_bgapi);
-
 	if (stream.param_event) {
 		switch_event_fire(&stream.param_event);
 	}
 
+	done:
 	switch_safe_free(reply);
 	switch_safe_free(acs->arg);
+	switch_atomic_dec(&ei_node->pending_bgapi);
 	switch_core_destroy_memory_pool(&pool);
+	switch_atomic_dec(&kazoo_globals.threads);
 	switch_safe_free(stream.data);
 
 	return NULL;
@@ -860,6 +867,7 @@ static switch_status_t handle_request_bgapi(switch_thread_start_t func, ei_node_
 	switch_thread_t *thread;
 	switch_threadattr_t *thd_attr = NULL;
 	switch_uuid_t uuid;
+	char uuid_str[SWITCH_UUID_FORMATTED_LENGTH + 1];
 	char cmd[MAXATOMLEN + 1];
 
 	if (ei_decode_atom_safe(buf->buff, &buf->index, cmd)) {
@@ -885,15 +893,24 @@ static switch_status_t handle_request_bgapi(switch_thread_start_t func, ei_node_
 	switch_threadattr_stacksize_set(thd_attr, SWITCH_THREAD_STACKSIZE);
 
 	switch_uuid_get(&uuid);
-	switch_uuid_format(acs->uuid_str, &uuid);
-	switch_thread_create(&thread, thd_attr, func, acs, acs->pool);
-
+	switch_uuid_format(uuid_str, &uuid);
+	switch_set_string(acs->uuid_str, uuid_str);
+	/* Pre-account detached work so node and module shutdown cannot pass it. */
 	switch_atomic_inc(&ei_node->pending_bgapi);
+	switch_atomic_inc(&kazoo_globals.threads);
+	if (switch_thread_create(&thread, thd_attr, func, acs, acs->pool) != SWITCH_STATUS_SUCCESS) {
+		switch_atomic_dec(&kazoo_globals.threads);
+		switch_atomic_dec(&ei_node->pending_bgapi);
+		switch_safe_free(acs->arg);
+		switch_core_destroy_memory_pool(&pool);
+		return SWITCH_STATUS_FALSE;
+	}
 
 	if (rbuf) {
 		ei_x_encode_tuple_header(rbuf, 2);
 		ei_x_encode_atom(rbuf, "ok");
-		_ei_x_encode_string(rbuf, acs->uuid_str);
+		/* The detached worker may already have destroyed acs->pool. */
+		_ei_x_encode_string(rbuf, uuid_str);
 	}
 
 	return SWITCH_STATUS_SUCCESS;
@@ -1052,6 +1070,7 @@ static switch_status_t handle_request_api(ei_node_t *ei_node, erlang_pid *pid, e
 static switch_status_t handle_request_event(ei_node_t *ei_node, erlang_pid *pid, ei_x_buff *buf, ei_x_buff *rbuf) {
 	char event_name[MAXATOMLEN + 1];
 	ei_event_stream_t *event_stream;
+	unsigned long stream_port;
 	int length = 0;
 	int i;
 
@@ -1060,8 +1079,16 @@ static switch_status_t handle_request_event(ei_node_t *ei_node, erlang_pid *pid,
 	}
 
 	switch_mutex_lock(ei_node->event_streams_mutex);
+	if (!switch_test_flag(ei_node, LFLAG_RUNNING) || !switch_test_flag(&kazoo_globals, LFLAG_RUNNING)) {
+		switch_mutex_unlock(ei_node->event_streams_mutex);
+		return SWITCH_STATUS_FALSE;
+	}
 	if (!(event_stream = find_event_stream(ei_node->event_streams, pid))) {
 		event_stream = new_event_stream(ei_node, pid);
+		if (!event_stream) {
+			switch_mutex_unlock(ei_node->event_streams_mutex);
+			return SWITCH_STATUS_FALSE;
+		}
 		/* ensure we are notified if the requesting processes dies so we can clean up */
 		ei_link(ei_node, ei_self(&kazoo_globals.ei_cnode), pid);
 	}
@@ -1073,6 +1100,7 @@ static switch_status_t handle_request_event(ei_node_t *ei_node, erlang_pid *pid,
 		}
 		add_event_binding(event_stream, event_name);
 	}
+	stream_port = get_stream_port(event_stream);
 	switch_mutex_unlock(ei_node->event_streams_mutex);
 
 	if (rbuf) {
@@ -1081,7 +1109,7 @@ static switch_status_t handle_request_event(ei_node_t *ei_node, erlang_pid *pid,
 
 		ei_x_encode_tuple_header(rbuf, 2);
 		ei_x_encode_string(rbuf, ei_node->local_ip);
-		ei_x_encode_ulong(rbuf, get_stream_port(event_stream));
+		ei_x_encode_ulong(rbuf, stream_port);
 	}
 
 	return SWITCH_STATUS_SUCCESS;
@@ -1450,9 +1478,6 @@ static switch_status_t handle_erl_msg(ei_node_t *ei_node, erlang_msg *msg, ei_x_
 static void *SWITCH_THREAD_FUNC receive_handler(switch_thread_t *thread, void *obj) {
 	ei_node_t *ei_node = (ei_node_t *) obj;
 
-	switch_atomic_inc(&kazoo_globals.threads);
-	switch_atomic_inc(&ei_node->receive_handlers);
-
 	switch_assert(ei_node != NULL);
 
 	switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_DEBUG, "Starting erlang receive handler %p: %s (%s:%d)\n", (void *)ei_node, ei_node->peer_nodename, ei_node->remote_ip, ei_node->remote_port);
@@ -1481,8 +1506,6 @@ static void *SWITCH_THREAD_FUNC handle_node(switch_thread_t *thread, void *obj) {
 	ei_received_msg_t *received_msg = NULL;
 	int fault_count = 0;
 
-	switch_atomic_inc(&kazoo_globals.threads);
-
 	switch_assert(ei_node != NULL);
 
 	switch_log_printf(SWITCH_CHANNEL_LOG, SWITCH_LOG_DEBUG, "Starting node request handler %p: %s (%s:%d)\n", (void *)ei_node, ei_node->peer_nodename, ei_node->remote_ip, ei_node->remote_port);
@@ -1671,13 +1694,40 @@ switch_status_t new_kazoo_node(int nodefd, ErlConnect *conn) {
 		switch_threadattr_create(&thd_attr, ei_node->pool);
 		switch_threadattr_detach_set(thd_attr, 1);
 		switch_threadattr_stacksize_set(thd_attr, SWITCH_THREAD_STACKSIZE);
-		switch_thread_create(&thread, thd_attr, receive_handler, ei_node, ei_node->pool);
+		switch_atomic_inc(&ei_node->receive_handlers);
+		switch_atomic_inc(&kazoo_globals.threads);
+		if (!switch_test_flag(&kazoo_globals, LFLAG_RUNNING)) {
+			switch_atomic_dec(&kazoo_globals.threads);
+			switch_atomic_dec(&ei_node->receive_handlers);
+			switch_clear_flag(ei_node, LFLAG_RUNNING);
+			destroy_node_handler(ei_node);
+			return SWITCH_STATUS_FALSE;
+		}
+		if (switch_thread_create(&thread, thd_attr, receive_handler, ei_node, ei_node->pool) != SWITCH_STATUS_SUCCESS) {
+			switch_atomic_dec(&kazoo_globals.threads);
+			switch_atomic_dec(&ei_node->receive_handlers);
+			switch_clear_flag(ei_node, LFLAG_RUNNING);
+			destroy_node_handler(ei_node);
+			return SWITCH_STATUS_FALSE;
+		}
 	}
 
 	switch_threadattr_create(&thd_attr, ei_node->pool);
 	switch_threadattr_detach_set(thd_attr, 1);
 	switch_threadattr_stacksize_set(thd_attr, SWITCH_THREAD_STACKSIZE);
-	switch_thread_create(&thread, thd_attr, handle_node, ei_node, ei_node->pool);
+	switch_atomic_inc(&kazoo_globals.threads);
+	if (!switch_test_flag(&kazoo_globals, LFLAG_RUNNING)) {
+		switch_atomic_dec(&kazoo_globals.threads);
+		switch_clear_flag(ei_node, LFLAG_RUNNING);
+		destroy_node_handler(ei_node);
+		return SWITCH_STATUS_FALSE;
+	}
+	if (switch_thread_create(&thread, thd_attr, handle_node, ei_node, ei_node->pool) != SWITCH_STATUS_SUCCESS) {
+		switch_atomic_dec(&kazoo_globals.threads);
+		switch_clear_flag(ei_node, LFLAG_RUNNING);
+		destroy_node_handler(ei_node);
+		return SWITCH_STATUS_FALSE;
+	}
 
 	return SWITCH_STATUS_SUCCESS;
 }
