[Service]
# A cloned applications unit must not start with the primary node's bind IP.
# Normal installer rendering replaces its environment before activation.
ExecCondition=/usr/bin/test ${KAZOO_ERLANG_DIST_IP} = 172.30.253.20
